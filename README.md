# quechoisir-comparator-scrapers
Implementing ETL pipelines to daily scrap the free comparators of the website www.quechoisir.org .  The data is stored to a cloud storage service (e.g. Google cloud BigQuery) and the logging is streamed to a cloud logging service (e.g. Google cloud Logging)ng 
